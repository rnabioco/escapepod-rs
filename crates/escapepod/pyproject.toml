[project]
name = "escapepod"
requires-python = ">=3.9"
dynamic = ["version"]

[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
profile = "dist"
