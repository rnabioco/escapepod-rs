[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "escapepod-cli"
dynamic = ["version"]
requires-python = ">=3.9"

[tool.maturin]
bindings = "bin"
profile = "dist"
